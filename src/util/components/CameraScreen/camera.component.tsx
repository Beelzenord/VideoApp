import { View,Text } from "react-native";
import { Camera,CameraType } from "expo-camera";
const CameraScreen = () =>{
    //
    return (
      <View style = {{flex:1, alignItems:'center', justifyContent:'center'}}>
        <Text>Camera Screens</Text>
      </View>
    );
  }

export default CameraScreen;